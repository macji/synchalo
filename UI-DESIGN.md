# SyncHalo 桌面端 UI/UX 设计文档

| 项目 | 内容 |
| --- | --- |
| 文档版本 | v0.14 |
| 更新日期 | 2026-09-03 |
| 适用平台 | macOS、Ubuntu 24.04 ARM64 |
| 设计阶段 | 结构与交互方案 |
| 推荐 UI 技术 | Tauri 2 + React + TypeScript + Vite |

## 1. 设计结论

SyncHalo 桌面端采用固定的左右分栏结构：

- 左侧是全局菜单，仅包含“粘贴板”“同步文件”“设置”。
- 右侧是当前菜单的内容区。
- “设置”页内包含临时同步码、我的设备列表，以及其他本机设置。
- 我的设备按“在线”和“离线”分组展示。
- UI 默认实时跟随操作系统的明暗模式。
- 整体风格是克制、高信息密度的系统工具，不使用渐变、毛玻璃、大面积插画或卡片堆叠。

按产品界面命名，用户可见文案统一使用“粘贴板”；代码和技术文档中仍可使用 `clipboard`。

## 2. UI 技术结论

SyncHalo 桌面端采用 `Tauri 2 + React + TypeScript + Vite + Rust 核心`：

```text
Tauri 桌面窗口
├── 系统 WebView
│   └── React + TypeScript + HTML/CSS
│       ├── 左右分栏和页面布局
│       ├── 历史虚拟列表
│       ├── 搜索、弹窗与 Toast
│       └── 跟随系统的明暗主题
│
└── Rust 核心
    ├── 剪贴板监听与回声抑制
    ├── mDNS 发现与设备配对
    ├── QUIC 文件传输
    ├── SQLite 与本地加密
    └── 托盘和平台能力
```

选择 Tauri 的原因：

- HTML/CSS 更适合精确实现固定侧栏、多行历史、滚动设置页和响应式细节。
- React 组件能复用历史行、状态标签、弹窗、Toast 和图标按钮。
- Web 语义和焦点管理便于实现键盘操作与读屏支持。
- Rust 同步核心保持独立，文件传输性能不依赖 WebView。
- Tauri 使用系统 WebView，不把完整浏览器运行时作为应用数据面。

明确边界：

- WebView 只负责展示、输入和临时 UI 状态。
- 文件字节、QUIC 连接、SQLite 连接、长期密钥和系统剪贴板监听都不能进入前端。
- Tauri IPC 只传 command、ViewModel、错误 DTO 和节流后的进度。
- 生产包只加载本地静态资源，不加载远程页面或 CDN 脚本。

## 3. 设计目标

### 3.1 用户进入 App 后应立即知道

1. 当前同步是否正常。
2. 有几台设备在线。
3. 最近同步了什么文本或文件。
4. 文件是否仍在传输、已经完成或失败。
5. 如何添加另一台设备。

### 3.2 设计原则

- **一眼可扫**：状态、来源设备和时间形成稳定视觉顺序。
- **历史优先**：右侧主体始终以列表为核心，而不是仪表盘。
- **危险操作克制**：删除图标清楚但不抢占主视觉，删除后可以撤销。
- **平台协调**：跟随系统主题和窗口装饰，保持一致的信息架构。
- **低打扰**：后台同步成功不弹模态框，只更新状态或使用轻量通知。
- **不靠颜色传达全部信息**：在线、完成、失败等状态同时使用文字和图标。

## 4. 全局窗口结构

### 4.1 窗口规格

| 项目 | 规格 |
| --- | --- |
| 默认窗口 | 1080 × 720 px |
| 最小窗口 | 860 × 560 px |
| 左侧栏 | 224 px；窗口较窄时缩到 184 px |
| 右侧内容 | 自适应，建议最小 640 px |
| 顶部栏 | 64 px |
| 内容水平边距 | 24 px |
| 列表最大阅读宽度 | 920 px；超出后保留右侧留白 |
| 窗口标题栏 | 使用系统原生标题栏和窗口按钮 |

### 4.2 全局字符线框

```text
┌──────────────────────────────────────────────────────────────────────────────┐
│                           系统原生标题栏                                     │
├──────────────────────┬───────────────────────────────────────────────────────┤
│                      │                                                       │
│  ◉  SyncHalo         │  当前页面标题                             页面操作    │
│  ● 同步正常 · 2 在线 │                                                       │
│                      ├───────────────────────────────────────────────────────┤
│  ┌────────────────┐  │                                                       │
│  │ ▣  粘贴板      │  │                                                       │
│  └────────────────┘  │                 当前页面内容                         │
│    ⇄  同步文件       │                                                       │
│    ⚙  设置           │                                                       │
│                      │                                                       │
│                      │                                                       │
│                      │                                                       │
│                      │                                                       │
│                      │                                                       │
│  ──────────────────  │                                                       │
│  ● 2 台在线           │                                                       │
│  ○ 1 台离线           │                                                       │
│  [暂停同步]           │                                                       │
└──────────────────────┴───────────────────────────────────────────────────────┘
```

### 4.3 左侧栏

顶部区域：

- SyncHalo 图标与名称。
- 全局状态：同步正常、已暂停、网络不可用或权限受限。
- 在线设备数量。

主菜单：

| 菜单 | 图标语义 | Badge |
| --- | --- | --- |
| 粘贴板 | 两张重叠的文本页 | 默认无；同步暂停时显示小点 |
| 同步文件 | 双向箭头或文件传输 | 正在传输的任务数量 |
| 设置 | 齿轮 | 待处理配对请求或权限异常 |

底部区域：

- 在线/离线数量摘要。
- “暂停同步”或“恢复同步”按钮。
- 版本号放入设置页，不长期占用侧栏空间。

菜单项高度 40 px，图标 18 px，图标与文字间距 10 px。当前项使用低饱和强调色背景和 3 px 左侧强调条；不要使用悬浮大卡片。

## 5. 粘贴板页面

### 5.1 页面目标

- 快速查看最近同步文本。
- 看清文本来自哪台设备、何时产生。
- 通过悬停后出现的预览、复制按钮查看完整内容或重新复制，单击内容本身不执行操作。
- 每条历史都可以单独删除。

### 5.2 页面线框

```text
┌──────────────────────────────────────────────────────────────────────────────┐
│  粘贴板历史                         [搜索历史] [收藏] [清空]              │
├──────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  今天                                                                        │
│  ┌────────────────────────────────────────────────────────────────────────┐  │
│  │   cargo test --workspace             [悬停：预览 复制 收藏 删除]      │  │
│  │   MacBook Pro · 本机 · 14:32                                          │  │
│  └────────────────────────────────────────────────────────────────────────┘  │
│  ┌────────────────────────────────────────────────────────────────────────┐  │
│  │   https://github.com/example/synchalo                                  │  │
│  │   ★ · Studio Ubuntu · 已接收 · 13:08                                  │  │
│  └────────────────────────────────────────────────────────────────────────┘  │
│  ┌────────────────────────────────────────────────────────────────────────┐  │
│  │ 会议结论：MVP 先覆盖 macOS 与 Ubuntu ARM64，其他平台后续……             │  │
│  │ Ubuntu · 已接收 · 10:46                                                │  │
│  └────────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
│  昨天                                                                        │
│  ┌────────────────────────────────────────────────────────────────────────┐  │
│  │ export RUST_LOG=synchalo=debug                                         │  │
│  │ MacBook Pro · 本机 · 昨天 21:17                                       │  │
│  └────────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘
```

线框中的边框用于表达区域；实际实现采用列表行和细分隔线，不把每条记录做成明显悬浮卡片。

### 5.3 顶部栏

- 标题：“粘贴板历史”。
- 搜索框：宽 220–280 px，支持 `Ctrl/Cmd + F` 聚焦。
- 粘贴板顶部栏不放置暂停/恢复按钮；全局暂停入口保留在左侧栏，暂停提示条仍包含“恢复”。
- “收藏”：位于“清空”左侧；按下时只显示收藏内容，再次点击恢复全部内容。
- “清空”：文字按钮；点击后使用确认弹窗，因为它影响全部历史。

搜索仅过滤本地历史，不影响系统剪贴板。搜索结果仍按 HLC 排序，显示产生时间。

### 5.4 历史行

每条历史最小高度 76 px，内容较长时最高 128 px，超过部分省略。

布局层级：

```text
第一层：文本内容，最多 3 行
第二层：收藏星标（可选）· 来源设备 · 本机/已接收 · 时间
右侧：悬停/聚焦后显示预览、复制、收藏、删除 icon，各有 32 × 32 px 点击区
```

交互规则：

- 单击历史内容不执行复制，允许用户正常选择文本。
- 双击不定义额外行为，避免平台差异。
- 预览、复制、收藏、删除图标默认透明且不响应鼠标；行悬停或按钮获得键盘焦点后出现。
- 点击预览图标打开包含完整原文的只读文本框。
- 点击复制图标写入本机系统粘贴板并产生提示“已复制”；若内容与最新一条相同，不创建历史副本或再次广播，复制不同的旧内容仍按一次新的本机复制处理。
- 删除图标正常为次级色，悬停时变为危险色。
- 单条删除不弹确认框，删除后底部出现 5 秒“已删除 · 撤销”。
- 删除同步关闭时只影响本机历史；相关设备均开启后，发送并接收对应历史记录的删除事件，撤销操作同步恢复。
- 右键菜单包含“预览”“复制”“收藏”“删除”。
- 每页固定 100 条；总数超过 100 时，列表底部显示总数、当前页、页码、上一页和下一页。分页区域上方不绘制装饰分隔线，点击任一分页按钮后滚动条立即回到列表开头。

### 5.5 内容展示

- 保留原始换行，但预览最多显示 3 行。
- URL 不自动渲染成高饱和蓝色链接，只有悬停或右键时提供“打开链接”。
- 密钥、密码等内容无法可靠自动识别，不提供虚假的安全标签。
- 超长无空格内容允许在字符边界换行，不能撑破列表。
- 已收藏内容在来源设备名前常驻显示“实心星标 ·”，未收藏不显示状态文字或图标。悬停星标仍只负责切换状态，收藏项不受自动清理影响。

### 5.6 空状态与异常状态

空状态：

```text
                  还没有粘贴板历史
        在任意已连接设备复制文本后，会显示在这里
                     [查看我的设备]
```

暂停状态在顶部栏下增加一条非模态提示：

```text
⏸ 粘贴板自动同步已暂停。本机仍可查看和复制历史。          [恢复]
```

平台受限时显示：

```text
! 当前 Linux 桌面环境不支持后台监听，请使用手动同步快捷键。 [了解详情]
```

## 6. 同步文件页面

### 6.1 页面目标

- 用左设备、右发送、下历史三个清晰区域完成目标选择、文件输入和历史管理。
- 把“选择文件”合并进拖入区；不显示粘贴按钮，在页面内按 `Ctrl/Cmd + V` 自动同步文件粘贴板内容。
- 清楚展示发送/接收方向和每台设备的状态。
- 支持重试、取消、打开和删除历史。

### 6.2 页面线框

```text
┌──────────────────────────────────────────────────────────────────────────────┐
│  同步文件                                         [搜索历史] [☆ 收藏]   │
├──────────────────────────────────────────────────────────────────────────────┤
│  ┌───────────────── 我的设备 ────────────────┐ ┌──────── 文件同步 ───────┐ │
│  │ Jason 的 MacBook Air  本机 [显示同步码] │ │ 未指定目标，将同步全部在线设备 │
│  │ ☐ Studio Ubuntu       在线              │ │ 把文件拖入或者直接粘贴文件│ │
│  │ ☐ Desk Pi             在线              │ │ [+ 选择文件]              │ │
│  │ ☐ Office Ubuntu       离线              │ │ 复制文件后按 ⌘V 自动同步 │ │
│  └──────────────────────────────────────────┘ └────────────────────────────┘ │
│  同步历史                                                                  │
│  ▣ SyncHalo-design.zip  传输中 67%              [再次同步] [☆] [取消]      │
│  ▤ notes.pdf                                      [再次同步] [★] [打开] […]  │
│  第 1 / 3 页 · 共 205 条 · 每页 100 条                  [‹] [1] [2] [3] [›] │
└──────────────────────────────────────────────────────────────────────────────┘
```

### 6.3 文件入口

- 左侧设备区域的本机固定第一项，显示实际设备名与“本机”；“显示同步码”在该行右侧触发模态弹层。弹层相对整个右侧内容区水平、垂直居中，蒙层覆盖右侧区域但不覆盖左侧导航，颜色固定为 `rgb(0 0 0 / 70%)`。
- 远端设备行同时是目标复选项，在线状态位于最右侧；暂停设备不可选。
- 拖入区本身是一个完整按钮，右侧以“+ 选择文件”强化可发现性；点击区域任意位置都打开绑定主窗口的原生文件选择框。
- 页面不放置“粘贴并同步”按钮；焦点不在输入框时按 `Ctrl/Cmd + V`，直接由 Rust 读取系统文件粘贴板并自动同步。没有目标时与选择、拖放、再次同步显示相同提示。
- 将文件拖入窗口时，拖入区切换为实线强调状态并显示“松开发送文件”，不再使用覆盖整个内容区的遮罩。
- 生产 Tauri 环境只消费原生拖放事件返回的绝对路径；浏览器 `File.name` 仅用于 Web 演示和测试。
- 拖入多个文件时，在开始发送前显示文件数量、总大小和目标设备。
- 默认不勾选设备，此时发送给当前全部在线设备；勾选一个或多个设备后，只发送给指定设备。
- 指定目标离线或传输中掉线时直接失败，不显示或持久化“等待设备上线”状态。
- 历史行右侧第一、第二个动作固定为“再次同步”和收藏；收藏项的文件图标和星标使用 Warning 色。
- 页面右上角收藏按钮沿用粘贴板页的按下状态、实心星标和二次点击恢复逻辑。
- 文件页不显示“全部 / 已发送 / 已接收 / 进行中 / 失败”标签筛选；搜索框移至页面右上角收藏按钮左侧，结构、清除按钮和快捷键与粘贴板页面一致。
- 文件历史与粘贴板历史采用同一分页结构：每页 100 条、总数、页码、上一页和下一页；搜索和收藏均请求后端第一页，点击页码后回到页面开头。

### 6.4 文件历史行

成功任务最小高度 72 px；进行中任务最小高度 96 px。

信息顺序：

1. 文件图标与文件名。
2. 文件大小、方向、设备和时间。
3. 进行中、失败或取消时的状态文字和适用操作；成功时不显示状态文字。
4. 仅在进行中时显示进度条、速度和剩余时间。

状态样式：

| 状态 | 图标 | 颜色用途 | 可用动作 |
| --- | --- | --- | --- |
| 传输中 | 双向箭头 | Accent | 暂停、取消 |
| 校验中 | 盾牌/校验 | Accent | 无 |
| 同步成功 | 不显示状态徽标 | — | 打开、定位 |
| 失败 | 感叹号 | Danger | 重试、详情 |
| 已取消 | 减号圆 | Secondary | 重新发送、删除历史 |

删除历史图标与粘贴板页一致。删除历史不删除最终文件；若要删除接收文件，必须从“更多”菜单中单独选择，并二次确认。

### 6.5 多设备展开

一个文件发送给多台设备时，点击设备摘要可展开：

```text
Studio Ubuntu                       14:33
Ubuntu           ⇄ 传输中 67%     82 MB/s
Office Mac       ! 失败（设备离线）
```

默认收起，避免主列表过高。

### 6.6 空状态

```text
                     还没有同步文件
          将文件拖到这里，或者复制文件后点击“粘贴文件”
                 使用上方区域拖入或选择文件
```

## 7. 设置页面

### 7.1 页面顺序

设置页右侧内容从上到下为：

1. 同步码。
2. 我的设备：在线设备。
3. 我的设备：离线设备。
4. 接收目录。
5. 同步与历史。
6. 启动与后台。
7. 诊断与关于。

首屏必须同时看到同步码和至少两行设备，不要把设备列表藏进二级页面。

### 7.2 设置页线框

```text
┌──────────────────────────────────────────────────────────────────────────────┐
│  设置                                                                        │
├──────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  添加设备                                                                    │
│  ┌────────────────────────────────────────────────────────────────────────┐  │
│  │ 同步码                    482 913                     剩余 00:48       │  │
│  │ 在另一台设备输入此码。同步码一次有效。          [复制] [刷新] [加入] │  │
│  └────────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
│  我的设备                                                                    │
│  在线 · 2                                                                    │
│  ──────────────────────────────────────────────────────────────────────────  │
│  ● MacBook Pro      本机 · macOS                  当前设备                  │
│  ● Studio Ubuntu    Ubuntu ARM64 · 192.168.1.18   刚刚同步          [•••]  │
│                                                                              │
│  离线 · 1                                                                    │
│  ──────────────────────────────────────────────────────────────────────────  │
│  ○ Ubuntu           Linux · 上次在线：昨天 22:14                    [•••]  │
│                                                                              │
│  文件接收                                                                    │
│  接收目录        /Users/me/Downloads                     [更改] [打开]       │
│                                                                              │
│  同步与历史                                                                  │
│  删除同步（默认关闭）                                        [ 开关 ]       │
│  收藏同步（默认关闭）                                        [ 开关 ]       │
│  保存历史        7 天                                           [ ▾ ]       │
│                                                                              │
│  启动与后台                                                                  │
│  开机时启动 SyncHalo                                         [ 开关 ]       │
│  关闭窗口后留在系统托盘                                     [ 开关 ]       │
│  自动更新                                                     [ 开关 ]       │
│  检查更新                                               [ 检查更新 ]       │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘
```

区块标题列采用紧凑窄栏：常规窗口宽 128 px，860 px 最小窗口宽 112 px。各区块只保留“添加设备”“我的设备”“文件接收”“同步与历史”“启动与后台”“当前设备”等标题，不在标题下放置说明性小字；具体帮助文案只在确有必要的控件内部出现。删除同步和收藏同步默认关闭；清空历史始终只影响本机。设置页不显示平台能力行，页脚只显示应用版本。

### 7.3 同步码

- 默认不永久展示固定码；用户进入设置页后点击“生成同步码”。
- 同步码为 6 位，显示为两组 3 位，提升口述和核对效率。
- 有效期与 PRD 保持一致，默认 60 秒。
- 显示倒计时、复制和刷新操作。
- 生成期间打开临时配对窗口；超时、使用一次或用户离开页面后自动失效。
- 复制后提示“同步码已复制，将在 48 秒后失效”。
- “加入”位于“生成同步码/刷新”右侧；点击后打开统一模态框，在其中输入六位同步码。页面删除原有行内输入框。
- 连续输错触发限流，但 UI 不暴露内部安全参数。

### 7.4 我的设备

设备按在线、离线分组，在线始终在前。

设备行字段：

```text
状态点 | 设备名称 | 平台 | IP/最近在线 | 最近同步 | 更多菜单
```

规则：

- 本机必须标记“当前设备”，且不能撤销自身。
- 在线使用绿色实心点和“在线”文字；离线使用灰色空心点和上次在线时间。
- 已在线设备收到离线事件后，前端等待 2 秒再切换显示；期间重新在线则取消离线更新。首次加载和离线转在线不延迟。
- 在线状态不能只依赖颜色。
- 更多菜单包含“重命名”“暂停向此设备同步”“撤销设备”。
- “撤销设备”使用危险确认弹窗，说明不会删除对方已有文件和历史。
- 设备较多时仍在同一页面滚动，不增加新的三级导航。

### 7.5 其他设置

- 接收目录显示经用户目录缩写后的可读路径；悬停显示完整路径。
- “打开”调用系统文件管理器。
- 历史保留支持 1 天、7 天、30 天、永久、不保存。
- 外观默认固定为“跟随系统”；MVP 不必提供独立主题切换。如果以后提供，选项为“跟随系统、浅色、深色”。
- 更新检查在启动约 5 秒后执行，之后每 30 分钟轮询；“检查更新”允许用户立即检查并反馈最新、失败、不支持或已忽略状态。关闭自动更新时使用“发现新版本”对话框展示版本、纯文本发布说明和“立即更新 / 忽略此版本”；忽略状态持久化至本机，更高版本出现时恢复提醒。开启时后台下载验签，完成后使用“更新已下载”对话框展示“安装并重启 / 稍后”。
- 同步文件与设置页“我的设备”标题右侧使用相同的紧凑刷新按钮；刷新期间图标旋转并禁用重复点击，完成后显示实际在线设备数。刷新必须重启 mDNS 发布/浏览并触发可信设备重连，不能仅重绘前端或把离线设备伪装成在线。
- 高级网络、日志和版本信息收在页面底部折叠区，不干扰主要操作。

## 8. 明暗主题与视觉系统

### 8.1 主题策略

- 默认使用 CSS media query `prefers-color-scheme`，并通过 `matchMedia` 监听系统主题实时变化。
- 系统从浅色切换到深色时，App 无需重启即可切换。
- 系统标题栏、边框和 App 内容保持同一主题。
- 强调色使用稳定的 SyncHalo Blue；MVP 不强求读取各平台系统强调色，因为 Linux 没有统一的跨桌面标准。
- 颜色切换只影响视觉，不改变业务状态。

### 8.2 浅色主题

| Token | 建议值 | 用途 |
| --- | --- | --- |
| `bg` | `#F5F6F8` | 窗口内容背景 |
| `sidebar` | `#ECEEF2` | 左侧栏 |
| `surface` | `#FFFFFF` | 输入框、提示区、同步码区域 |
| `hover` | `#E7EBF2` | 列表和菜单悬停 |
| `selected` | `#DDE9FF` | 当前菜单、选中行 |
| `border` | `#D9DDE5` | 分隔线和控件边框 |
| `text` | `#1B1E24` | 主文字 |
| `text-secondary` | `#69707D` | 来源、时间、辅助信息 |
| `accent` | `#3478F6` | 主操作、传输中 |
| `success` | `#2F9D5A` | 在线、完成 |
| `warning` | `#B87516` | 收藏、受限 |
| `danger` | `#D34B4B` | 失败、删除悬停 |

### 8.3 深色主题

| Token | 建议值 | 用途 |
| --- | --- | --- |
| `bg` | `#17191D` | 窗口内容背景 |
| `sidebar` | `#1D2025` | 左侧栏 |
| `surface` | `#23262C` | 输入框、提示区、同步码区域 |
| `hover` | `#292D34` | 列表和菜单悬停 |
| `selected` | `#243A60` | 当前菜单、选中行 |
| `border` | `#353A43` | 分隔线和控件边框 |
| `text` | `#F0F2F5` | 主文字 |
| `text-secondary` | `#A2A9B4` | 来源、时间、辅助信息 |
| `accent` | `#6EA8FF` | 主操作、传输中 |
| `success` | `#55C67C` | 在线、完成 |
| `warning` | `#E0A24A` | 收藏、受限 |
| `danger` | `#FF7272` | 失败、删除悬停 |

### 8.4 字体

优先加载平台系统 UI 字体：

- macOS：SF Pro / PingFang SC。
- Linux：系统 Sans / Noto Sans CJK SC。
- 找不到系统 CJK 字体时使用 App 内置的开源 CJK fallback。

正文 14 px，辅助信息 12 px，页面标题 20 px/600，区块标题 14 px/600。代码内容可使用平台等宽字体，但不强制自动判断所有文本类型。

### 8.5 间距、圆角和阴影

- 使用 4 px 基础网格：4、8、12、16、24、32。
- 控件圆角 6 px，大型提示区 8 px。
- 历史列表本身不使用明显圆角卡片。
- 仅弹出菜单和浮层使用轻阴影；主页面依赖背景层级和细分隔线。
- 分隔线 1 px，避免重边框。

### 8.6 图标

- 使用统一的 18 px 线性 SVG 图标集，不使用 emoji 作为正式 UI 图标。
- 删除使用垃圾桶轮廓，正常态为次级色，悬停/聚焦时为危险色。
- 图标按钮最小点击区域 32 × 32 px。
- 无文字的图标按钮必须提供 tooltip 和无障碍名称。

## 9. 反馈、弹窗和动效

### 9.1 Toast

底部居中显示，持续 3–5 秒：

```text
[✓ 已复制]
[✓ 文件已加入发送队列]
[历史已删除] [撤销]
[! 磁盘空间不足] [查看详情]
```

同一时间最多显示两个，新消息合并重复状态。

### 9.2 统一模态框

`ModalDialog` 统一提供蒙层、居中面板、标题、关闭按钮、Esc、点击空白关闭、焦点和操作区。以下交互复用同一组件：

- 清空全部历史等确认操作。
- 同步文件页显示同步码。
- 设置页输入同步码加入设备。

确认型模态框仅用于不可轻易撤销的操作：

- 清空全部历史。
- 撤销设备。
- 删除本地接收文件。
- 取消正在传输且需要删除临时文件的任务。

删除单条历史不弹确认框，使用撤销 Toast。

### 9.3 动效

- 菜单、列表悬停：120 ms。
- 主题切换：不做跨色渐变，立即切换，避免闪烁和文字对比度中间态。
- 展开设备详情：160 ms 高度动画。
- 进度条：平滑追踪数据进度，不伪造进度。
- 用户开启“减少动态效果”时关闭非必要动画。

## 10. 键盘与无障碍

### 10.1 快捷键

| 快捷键 | 行为 |
| --- | --- |
| `Ctrl/Cmd + 1` | 打开粘贴板 |
| `Ctrl/Cmd + 2` | 打开同步文件 |
| `Ctrl/Cmd + ,` | 打开设置 |
| `Ctrl/Cmd + F` | 聚焦当前页面搜索 |
| `Ctrl/Cmd + V` | 在同步文件页读取文件粘贴板 |
| `Enter` | 激活当前聚焦行或按钮 |
| `Esc` | 关闭弹窗、菜单或拖放覆盖层 |

### 10.2 无障碍要求

- 所有功能可以仅用键盘完成。
- 聚焦环 2 px，不能只使用背景色变化。
- 主文字与背景对比度目标至少 4.5:1。
- 状态同时使用图标、文字和颜色。
- 支持系统缩放；150% 缩放下不裁切按钮文字。
- 历史列表使用语义化可聚焦行，删除按钮作为独立子控件。
- 使用语义化 HTML、可访问名称和正确的焦点顺序，并在 macOS 与 Ubuntu ARM64 进行实际键盘与读屏验证。

## 11. Tauri Web UI 技术设计

### 11.1 目录结构

```text
apps/
└── desktop/
    ├── package.json
    ├── vite.config.ts
    ├── index.html
    ├── src/
    │   ├── main.tsx
    │   ├── App.tsx
    │   ├── routes.ts
    │   ├── api/
    │   │   ├── commands.ts
    │   │   ├── events.ts
    │   │   └── types.ts
    │   ├── pages/
    │   │   ├── ClipboardPage.tsx
    │   │   ├── FilesPage.tsx
    │   │   └── SettingsPage.tsx
    │   ├── components/
    │   │   ├── Sidebar.tsx
    │   │   ├── Toolbar.tsx
    │   │   ├── HistoryRow.tsx
    │   │   ├── DeviceRow.tsx
    │   │   ├── TransferRow.tsx
    │   │   ├── StatusBadge.tsx
    │   │   ├── IconButton.tsx
    │   │   ├── ToastRegion.tsx
    │   │   └── ConfirmDialog.tsx
    │   ├── state/
    │   └── styles/
    │       ├── tokens.css
    │       ├── globals.css
    │       └── components.css
    └── src-tauri/
        ├── Cargo.toml
        ├── tauri.conf.json
        ├── capabilities/
        │   └── default.json
        └── src/
            ├── main.rs
            ├── lib.rs
            ├── commands.rs
            ├── events.rs
            └── tray.rs

crates/
├── core/
├── network/
├── transfer/
├── clipboard/
├── storage/
└── platform/
```

### 11.2 UI 状态

```ts
type Route = "clipboard" | "files" | "settings";

interface UiState {
  route: Route;
  syncStatus: SyncStatusView;
  onlineDevices: DeviceView[];
  offlineDevices: DeviceView[];
  clipboardHistory: ClipboardItemView[];
  fileHistory: FileItemView[];
  clipboardSearch: string;
  fileFilter: FileFilter;
  modal: ModalState | null;
  toasts: ToastView[];
}
```

前端只保存用于显示的 projection 和临时交互状态，不持有 QUIC 连接、数据库连接、私钥、文件句柄或文件内容。设备、历史与任务的权威状态始终在 Rust。

### 11.3 Web 前端与 Rust 通信

```text
React 页面与组件
   │ 类型化 commands.ts
   ▼
Tauri invoke
   │ 参数验证与错误映射
   ▼
Rust Application Service
   ├── DiscoveryService
   ├── ClipboardService
   ├── TransferService
   └── StorageService
   │
   ▼ Tauri event / channel
前端 store 精确更新对应 ViewModel
```

Command 用于有明确请求和响应的动作：

```text
list_clipboard_history
copy_history_item
delete_clipboard_item
paste_files
enqueue_files
retry_transfer
generate_pairing_code
join_with_code
revoke_device
update_settings
```

Event 用于离散状态变化，channel 用于有顺序的持续进度：

```text
clipboard_history_added
clipboard_history_deleted
device_state_changed
pairing_code_updated
transfer_added
transfer_progress
transfer_state_changed
settings_changed
user_facing_error
```

- 页面组件不得直接拼接 command 名称，全部经 `src/api` 封装。
- Command 与事件 payload 使用独立 DTO，不直接暴露领域对象。
- React 组件卸载时必须注销监听器，避免重复订阅和内存泄漏。
- Rust 对来自 WebView 的 ID、路径、分页游标和设置值重新校验。

### 11.4 渲染与性能

- 历史列表使用虚拟滚动，只渲染可见行和少量 overscan。
- 列表行使用稳定 ID 和局部状态更新，进度变化不能触发整页重渲染。
- Rust 将每个活跃任务的进度合并并限制为每秒 5–10 次。
- 文件字节、哈希流和网络缓冲区不进入 WebView 或 Tauri IPC。
- 剪贴板正文由 Rust 分页解密，只向前端发送当前页面需要的条目。
- 搜索敏感历史由 Rust 在受控内存中执行，前端不维护永久明文全文索引。
- 前端构建不依赖运行时 Node.js；Node/pnpm 只用于开发和产物构建。

### 11.5 平台集成

| 能力 | 方案 |
| --- | --- |
| 文件拖放 | 使用 Tauri 窗口/WebView 拖放事件，由 Rust 校验路径后返回文件摘要 |
| 选择文件/目录 | Tauri dialog 插件或受控 Rust command 调用系统对话框 |
| 系统主题 | CSS `prefers-color-scheme` + `matchMedia` 实时监听 |
| 系统托盘 | Rust 侧 Tauri tray API |
| 打开/定位文件 | Tauri opener 插件或平台模块；只接受 Rust 已确认的任务 ID |
| 开机启动 | Tauri autostart 插件，状态由 Rust 持久化并回传 |
| 文本剪贴板监听 | PRD 中的平台原生 `ClipboardBackend`，不使用浏览器 Clipboard API 代替 |
| 文件粘贴板读取 | macOS NSPasteboard、Ubuntu X11/Wayland `text/uri-list`，由 Rust 处理 |

macOS 左键点击状态栏图标直接显示、还原并聚焦主窗口，右键打开菜单。Linux 托盘与 WebKitGTK 需要对目标发行版进行打包测试；AppIndicator 不提供图标点击事件，因此点击图标打开菜单，并以首项“打开 SyncHalo”完成激活。

## 12. 技术选型边界

| 维度 | SyncHalo 决策 |
| --- | --- |
| 桌面壳 | Tauri 2 |
| UI | React + TypeScript + HTML/CSS，Vite 构建静态 SPA |
| 渲染器 | macOS WKWebView、Ubuntu WebKitGTK |
| UI 与核心通信 | Tauri command + event/channel |
| 数据面 | 全部留在 Rust，不进入 WebView 或 IPC |
| 远程内容 | 默认禁止；不加载远程页面、脚本或 CDN 资源 |
| 权限 | 使用 Tauri capabilities 与应用权限清单，按窗口、command 和插件最小授权 |
| 主题 | CSS variables + 跟随系统明暗模式 |

选择 Tauri 会引入前后端 IPC 和多套系统 WebView 的兼容测试，但换来更成熟的排版、CJK 文本、焦点管理和 Web 无障碍能力。对 SyncHalo 而言，这个取舍已确定；除非后续出现无法接受的 WebView 平台缺陷，否则不再并行维护其他 UI 技术路线。

## 13. UI 验收标准

1. 主窗口始终采用左侧菜单、右侧内容的两栏布局。
2. 左侧仅显示粘贴板、同步文件、设置三个一级菜单。
3. 粘贴板历史行点击不复制；已收藏行在来源设备名前显示“实心星标 ·”，未收藏不显示状态，悬停或键盘聚焦后显示预览、复制、收藏、删除图标；复制与最新一条相同的内容不重复同步。
4. 删除单条历史后可在 5 秒内撤销。
5. 粘贴板顶部无重复暂停按钮；收藏筛选可在收藏内容与全部内容之间切换，历史超过 100 条时按每页 100 条分页；分页区无顶部装饰线，点击页码后回到列表开头。
6. 同步文件页采用左设备、右发送、下历史三分区；本机固定第一项；同步码弹层在整个右侧区域居中，并使用 70% 黑色蒙层。
7. 页面无独立粘贴按钮，按 `Ctrl/Cmd + V` 自动同步；没有勾选目标时发送给全部在线设备，勾选后只发送给指定设备；没有在线或指定目标时显示统一提示。
8. 文件页无状态标签筛选，搜索框位于右上角收藏左侧；历史支持再次同步、持久化收藏、顶部收藏筛选及每页 100 条的 Rust/SQLite 后端分页。
9. 文件历史清楚显示方向、设备、进度和错误；成功项不显示“已完成”，进行中、失败和取消项显示状态。
10. 设置页首屏展示同步码以及按在线/离线分组的设备列表；“加入”使用统一模态框，页面无行内加入输入框、自动同步粘贴板开关、通知开关和平台能力行；“同步与历史”展示默认关闭的删除同步与收藏同步，“启动与后台”提供默认开启的自动下载开关与手动检查入口，关闭时仍保持 30 分钟检查并只提醒；页脚只显示应用版本；区块标题下无重复辅助说明，标题列在常规/最小窗口分别不超过 128/112 px。
11. 系统切换明暗模式后，窗口和内容无需重启即可正确切换。
12. 深浅主题下所有状态文字可读，状态不只通过颜色表达。
13. 860 × 560 和 150% 系统缩放下不裁切主要操作。
14. macOS 与 Ubuntu ARM64 均可用键盘完成导航、搜索、粘贴文件和删除历史。
15. 大文件传输时 UI 保持流畅，进度更新不会导致持续高 CPU。
16. 关闭窗口后按设置进入托盘，后台同步不中断。
17. Linux Wayland 剪贴板能力受限时显示明确状态，不静默失败。
18. macOS 点击状态栏图标直接激活主窗口；Ubuntu 托盘菜单可通过首项激活主窗口。

## 14. 实施顺序

1. 建立 Tauri 2 + React/TypeScript/Vite 工程、窗口与最小 capabilities。
2. 建立 CSS tokens、系统主题监听和全局字体栈。
3. 完成 Sidebar、Toolbar、IconButton、Toast 等基础组件。
4. 使用静态数据实现三个页面和字符线框对应布局。
5. 建立类型化 commands/events API，并接入 Rust ViewModel DTO。
6. 接入 SQLite 历史分页、虚拟列表和设备状态。
7. 接入文件拖放、文件选择和粘贴文件。
8. 接入托盘、关闭隐藏、开机启动与系统通知。
9. 完成 CSP、capabilities、WKWebView/WebKitGTK、CJK 字体、缩放和无障碍测试。

## 15. 技术参考

- [Tauri 架构](https://v2.tauri.app/concept/architecture/)
- [Tauri 前端配置](https://v2.tauri.app/start/frontend/)
- [Tauri WebView 版本与平台差异](https://v2.tauri.app/reference/webview-versions/)
- [Tauri Commands、Events 与 Channels](https://v2.tauri.app/develop/calling-rust/)
- [Tauri Capabilities](https://v2.tauri.app/security/capabilities/)
- [Tauri Content Security Policy](https://v2.tauri.app/security/csp/)
- [Tauri 系统托盘](https://v2.tauri.app/learn/system-tray/)
